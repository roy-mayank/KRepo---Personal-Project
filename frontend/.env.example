# Local dev
VITE_API_URL=http://127.0.0.1:8000

# Production (Railway frontend service — set BEFORE build; Vite bakes this in)
# VITE_API_URL=https://your-backend.up.railway.app   # must be https on Railway
